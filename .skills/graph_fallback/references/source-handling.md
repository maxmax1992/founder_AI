# Fallback Source Handling

Prefer compiled wiki pages for synthesis, then use raw sources for provenance and conflict checks.

When a source is marked `ready`, it can support core advisor claims. When a source is marked `needs_review` or has an extraction note, treat it as weak evidence and say that it needs review before using it as a stable advisor claim.

Do not fabricate advisor-specific frameworks from thin or missing sources.
